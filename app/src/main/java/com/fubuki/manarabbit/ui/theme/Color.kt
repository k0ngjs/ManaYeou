package com.fubuki.manarabbit.ui.theme

import androidx.compose.ui.graphics.Color

// ── 강조색 ──────────────────────────────────────────────
val Accent        = Color(0xFF1ED760)   // 메인 강조색
val AccentDark    = Color(0xFF17B84E)   // 다크 테마 primaryContainer 텍스트용
val AccentContainerDark  = Color(0xFF003A17)   // 다크 테마 primaryContainer 배경
val AccentContainerLight = Color(0xFFB6F5CE)   // 라이트 테마 primaryContainer 배경

// ── 다크 테마 무채색 ────────────────────────────────────
val Dark0   = Color(0xFF1A1A1A)   // 기본 배경
val Dark1   = Color(0xFF222222)   // surface
val Dark2   = Color(0xFF2C2C2C)   // surfaceVariant
val Dark3   = Color(0xFF353535)   // surfaceContainer
val Dark4   = Color(0xFF3F3F3F)   // surfaceContainerHigh
val Dark5   = Color(0xFF494949)   // surfaceContainerHighest
val DarkText        = Color(0xFFF0F0F0)
val DarkTextSub     = Color(0xFF9A9A9A)
val DarkOutline     = Color(0xFF525252)
val DarkOutlineVar  = Color(0xFF353535)

// ── 라이트 테마 무채색 ───────────────────────────────────
val Light0  = Color(0xFFFFFFFF)   // surface (바, 다이얼로그, 카드)
val Light1  = Color(0xFFEBEBEB)   // 기본 배경 (회색 페이지)
val Light2  = Color(0xFFF5F5F5)   // surfaceVariant / surfaceContainerLow (카드, 입력창)
val Light3  = Color(0xFFE3E3E3)   // surfaceContainer
val Light4  = Color(0xFFD8D8D8)   // surfaceContainerHigh
val Light5  = Color(0xFFCCCCCC)   // surfaceContainerHighest
val LightText       = Color(0xFF111111)
val LightTextSub    = Color(0xFF5E5E5E)
val LightOutline    = Color(0xFFBBBBBB)
val LightOutlineVar = Color(0xFFD8D8D8)
